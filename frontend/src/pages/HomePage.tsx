import { DailyBriefing } from '../components/home/DailyBriefing';

/** Home hub: briefing overview grid (see `DailyBriefing`). */
export function HomePage() {
  return (
    <div className="bg-codex-bg">
      <DailyBriefing />
    </div>
  );
}
