<VirtualHost *:80>
    ServerAdmin webmaster@localhost
    ServerName localhost
    DocumentRoot /var/www/html/public

    # Docker / Compose set CODEX_API_KEY in the container env; Apache must pass it into PHP (getenv).
    PassEnv CODEX_API_KEY
    PassEnv FRONTEND_URL
    PassEnv ANTHROPIC_API_KEY
    PassEnv GOOGLE_CLIENT_ID
    PassEnv GOOGLE_CLIENT_SECRET
    PassEnv GOOGLE_REDIRECT_URI 

    <Directory /var/www/html/public>
        Options Indexes FollowSymLinks
        AllowOverride All
        Require all granted
        FallbackResource /index.php
    </Directory>

    ErrorLog ${APACHE_LOG_DIR}/error.log
    CustomLog ${APACHE_LOG_DIR}/access.log combined
</VirtualHost>
